<div class="row justify-content-center">
    <div class="col-md-8">
        {{ form_start(form) }}

        {{ form_widget(form.receiptFile, {'attr': {'data-src': vich_uploader_asset(product, 'receiptFile')|imagine_filter('list_thumbnail')}}) }}
        
        {{ form_end(form) }}
            
    </div>
</div>


